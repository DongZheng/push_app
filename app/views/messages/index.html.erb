<h1>Chat</h1>

<ul id="chat">
	<!-- the Rails helper 'render' will look for the app/views/messages/_message.html.erb, and insert the result here -->
	<%= render @messages %>
</ul>
	
<%= form_for Message.new, :remote => true do |f| %>
	<%= f.text_field :content %>
	<%= f.submit "Send" %>
<% end %>